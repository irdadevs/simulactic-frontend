export type AdminSection = "overview" | "entities" | "users" | "donations" | "logs" | "bans" | "metrics" | "traffic";

export const adminSections: AdminSection[] = [
  "overview",
  "entities",
  "users",
  "donations",
  "logs",
  "bans",
  "metrics",
  "traffic",
];
